-- Step 4: Disable email confirmation for easier testing
-- Run this in your Supabase SQL Editor
-- NOTE: You can enable this later in production if needed

-- This is handled in Supabase Dashboard > Authentication > Settings
-- Set "Enable email confirmations" to OFF for easier testing
-- You can enable it later for production

SELECT 'Email confirmation settings should be configured in Supabase Dashboard > Authentication > Settings' as note;
